version: '3'
services:
  twitter-video-downloader:
    image: twitter-video-downloader
    build:
      context: https://github.com/Aoismail/XDownloader.git
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "3000:3000"
