version: '3'
services:
  twitter-video-downloader:
    image: twitter-video-downloader
    build:
      context: https://github.com/Aoismail/XDownloader/
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
